name: Backend CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-test:
    name: Restore, Build, Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore Pastil.sln

      - name: Release build
        run: dotnet build Pastil.sln --configuration Release --no-restore

      - name: Application tests
        run: dotnet test Application.Tests/Application.Tests.csproj --configuration Release --no-restore --logger "console;verbosity=normal"
